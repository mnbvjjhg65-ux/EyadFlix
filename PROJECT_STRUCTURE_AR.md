# 📚 EyadFlix - بنية المشروع

## 📂 هيكل الملفات

```
lib/
├── main.dart                          # نقطة الدخول الرئيسية للتطبيق
│
├── core/                             # الطبقة الأساسية
│   ├── constants/
│   │   └── app_constants.dart        # ثوابت التطبيق والـ API
│   ├── errors/
│   │   └── exceptions.dart           # استثناءات مخصصة
│   └── utils/
│       └── enums.dart                # تعريفات الأنواع
│
├── data/                             # طبقة البيانات
│   ├── datasources/                  # مصادر البيانات (API، قاعدة البيانات)
│   ├── models/                       # نماذج البيانات
│   │   ├── addon_manifest.dart       # بيانات ملف البرنامج الإضافي
│   │   ├── meta_model.dart           # معلومات المحتوى
│   │   ├── stream_model.dart         # مصادر التشغيل
│   │   ├── subtitle_model.dart       # الترجمات
│   │   ├── installed_addon.dart      # البرامج المثبتة (Hive)
│   │   ├── watch_history.dart        # السجل (Hive)
│   │   └── *.g.dart                  # ملفات التسلسل التلقائي
│   └── repositories/                 # مستودعات البيانات
│
├── domain/                           # طبقة المنطق
│   ├── entities/                     # كيانات المجال
│   ├── repositories/                 # واجهات المستودع
│   └── usecases/                     # حالات الاستخدام
│
├── presentation/                     # طبقة العرض
│   ├── pages/                        # الصفحات الرئيسية
│   │   ├── home_page.dart           # الصفحة الرئيسية
│   │   ├── addons_page.dart         # إدارة البرامج
│   │   ├── library_page.dart        # المكتبة والمفضلة
│   │   ├── settings_page.dart       # الإعدادات
│   │   ├── catalog_page.dart        # عرض الفهرس
│   │   ├── media_detail_page.dart   # تفاصيل المحتوى
│   │   ├── video_player_page.dart   # مشغل الفيديو
│   │   └── addon_detail_page.dart   # تفاصيل البرنامج
│   ├── widgets/                      # المكونات المستخدمة مكرراً
│   │   ├── bottom_nav_bar.dart      # شريط التنقل
│   │   └── common_widgets.dart      # عناصر مشتركة
│   └── providers/                    # مديرو الحالة (Riverpod)
│       ├── app_providers.dart       # موفري التطبيق العام
│       └── library_providers.dart   # موفري المكتبة
│
├── services/                        # طبقة الخدمات
│   ├── addon_service.dart           # خدمة بروتوكول Stremio
│   ├── local_storage_service.dart   # خدمة التخزين المحلي
│   ├── localization_service.dart    # خدمة التعريب
│   └── theme_service.dart           # خدمة المواضيع
│
└── EXAMPLES.dart                    # أمثلة الاستخدام
```

## 🎯 طبقات المشروع

### 1️⃣ Core Layer (الطبقة الأساسية)
**الهدف**: توفير أساس موحد للتطبيق بأكمله

```dart
// مثال: استخدام الثوابت
String apiBaseUrl = AppConstants.stremioApiBaseUrl;

// مثال: معالجة الأخطاء
try {
  // عملية
} on NetworkException catch (e) {
  // خطأ الشبكة
} on AddonException catch (e) {
  // خطأ البرنامج
}
```

### 2️⃣ Data Layer (طبقة البيانات)
**الهدف**: إدارة البيانات من مصادر مختلفة

**النماذج**:
- `AddonManifest`: تفاصيل البرنامج الإضافي
- `MetaModel`: معلومات المحتوى (الأفلام، المسلسلات)
- `StreamModel`: مصادر التشغيل (روابط الفيديو)
- `SubtitleModel`: الترجمات المتعددة اللغات
- `InstalledAddon`: البرامج المثبتة (محفوظ في Hive)
- `WatchHistoryItem`: سجل المشاهدة (محفوظ في Hive)

### 3️⃣ Services Layer (طبقة الخدمات)
**الهدف**: تنفيذ المنطق التجاري

#### AddonService
يتعامل مع بروتوكول Stremio:

```dart
final service = AddonService();

// جلب بيانات البرنامج
final manifest = await service.fetchManifest(addonUrl);

// جلب الفهرس
final catalog = await service.fetchCatalog(
  addonUrl,
  ContentType.movie,
  'top100',
);

// جلب التفاصيل
final meta = await service.fetchMeta(
  addonUrl,
  ContentType.movie,
  'imdb_id',
);

// جلب مصادر التشغيل
final streams = await service.fetchStreams(
  addonUrl,
  ContentType.movie,
  'imdb_id',
);

// جلب الترجمات
final subtitles = await service.fetchSubtitles(
  addonUrl,
  ContentType.movie,
  'imdb_id',
);
```

#### LocalStorageService
يدير التخزين المحلي:

```dart
final storage = LocalStorageService();

// البرامج المثبتة
await storage.saveAddon(installedAddon);
final addon = await storage.getAddon(id);
final addons = await storage.getAllAddons();
await storage.deleteAddon(id);

// السجل
await storage.saveWatchHistory(item);
final history = await storage.getAllWatchHistory();

// المفضلة
await storage.addToFavorites(mediaId);
final isFav = await storage.isFavorite(mediaId);
```

### 4️⃣ Presentation Layer (طبقة العرض)
**الهدف**: واجهة المستخدم

#### الصفحات:
- **HomePage**: الصفحة الرئيسية بـ 4 تبويبات
- **AddonsPage**: إضافة وإدارة البرامج
- **CatalogPage**: عرض الأفلام والمسلسلات
- **MediaDetailPage**: تفاصيل المحتوى الكاملة
- **VideoPlayerPage**: مشغل الفيديو
- **LibraryPage**: المفضلة والسجل
- **SettingsPage**: اللغة والمظهر

## 🔄 تدفق البيانات

```
User Action
    ↓
Page/Widget
    ↓
Riverpod Provider
    ↓
Service Layer (AddonService, LocalStorageService)
    ↓
Data Models
    ↓
Storage/API
    ↓
Update UI
```

## 📱 الصفحات الرئيسية

### HomePage (الرئيسية)
- 4 تبويبات سفلية
- عرض البرامج المفعلة
- الوصول السريع لجميع الميزات

### AddonsPage (البرامج)
- إضافة برنامج إضافي جديد
- تفعيل/تعطيل البرامج
- حذف البرامج المثبتة
- عرض معلومات البرنامج

### CatalogPage (الفهرس)
- عرض المحتوى في شبكة
- البحث والتصفية
- التمرير اللانهائي
- صور معاد استخدامها

### MediaDetailPage (التفاصيل)
- صورة الخلفية
- الملصق (Poster)
- الملخص والمعلومات
- قائمة الحلقات (للمسلسلات)
- زر التشغيل والمفضلة

### VideoPlayerPage (المشغل)
- التشغيل الكامل
- اختيار الترجمات
- التحكم بالسرعة
- تخطي المقدمة الموسيقية

## 🌐 اللغات المدعومة

### العربية (RTL)
- `assets/translations/ar.json`
- 150+ نص
- دعم كامل للغات من اليمين لليسار

### الإنجليزية (LTR)
- `assets/translations/en.json`
- 150+ نص
- اللغة الافتراضية

### الاستخدام:
```dart
// الحصول على النص
String title = context.tr('homeScreen.title');

// التبديل بين اللغات
ref.read(languageProvider.notifier).state = 'ar';
```

## 🎨 المواضيع

- **Material Design 3**: تصميم حديث احترافي
- **المظهر الفاتح**: ألوان دافئة وناعمة
- **المظهر الداكن**: ألوان هادئة ومريحة للعين
- **دعم كامل RTL**: للواجهات العربية

## 🚀 البدء السريع

```bash
# 1. تثبيت الاعتماديات
flutter pub get

# 2. إنشاء ملفات التسلسل
flutter pub run build_runner build --delete-conflicting-outputs

# 3. تشغيل التطبيق
flutter run

# 4. إضافة برنامج اختياري
# انتقل إلى تبويب "البرامج" وأضف:
# https://torrentio.strem.fun/manifest.json
```

## 📊 الإحصائيات

- **34 ملف Dart**: كود مكتوب بعناية
- **7 ملفات توثيق**: شروحات شاملة
- **150+ نصوص مترجمة**: دعم ثنائي اللغة
- **10+ موفرو بيانات**: إدارة حالة متقدمة
- **8+ صفحات**: واجهة مستخدم كاملة
- **660KB حجم المشروع**: حجم مناسب

## 🔗 الروابط المهمة

- [تطبيق Stremio](https://www.stremio.com)
- [بروتوكول Stremio](https://stremio.github.io/stremio-addon-sdk/)
- [Flutter](https://flutter.dev)
- [Riverpod](https://riverpod.dev)

---

**المطور**: Eyad AI Juhani
**الإصدار**: 1.0.0
**التاريخ**: 2024
